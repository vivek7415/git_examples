rm -f ./.git/index.lock => resetting

git config --global user.name "vivek7415"

git config --global user.email "viveksngh443@gmail.com"

git status => status of current directory -  how many files are in staging area

git add "filename.extension" => add file to staging area

git diff => tracks the changes done in file who are in staging area

git remote add git1 https://github.com/vivek7415/git_examples.git => git1 is alais name for the url

git push git1 master => push the files